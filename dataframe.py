#A dataframe contains tabular data, which is organized into rows and columns. It is a powerful data structure provided by the pandas library in Python, and it allows for easy manipulation and analysis of data. Each column in a DataFrame can have a different data type (e.g., integers, floats, strings), and it provides various functions for filtering, grouping, and summarizing data.
import pandas as pd

# Read the CSV file into a DataFrame
df = pd.read_csv('leads.csv')
print(df)




